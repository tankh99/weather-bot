# Telegram Weather Bot in Python
### Troublshooting
#### python is loading libcrypto in an unsafe way
Reason being that pip is outdated. Run the following commands
```
pip install --upgrade pip
pip install --upgrade pyOpenSSL
```
